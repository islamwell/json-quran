name = "nq-media-proxy"
main = "worker.js"
compatibility_date = "2024-09-01"

# Environment variables
[vars]
ORIGIN_HOSTNAME = "origin.nq-international.com"

# Route pattern when deployed as a standalone Worker on your zone:
# routes = [
#   { pattern = "nq-international.com/wp-content/uploads/*", zone_name = "nq-international.com" }
# ]
